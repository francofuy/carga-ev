import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.francofuy.cargaev',
  appName: 'Carga EV',
  webDir: 'dist-capacitor',
};

export default config;
